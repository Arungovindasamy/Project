import { SamplepimePipe } from './samplepipe.pipe';

describe('SamplepimePipe', () => {
  it('create an instance', () => {
    const pipe = new SamplepimePipe();
    expect(pipe).toBeTruthy();
  });
});
